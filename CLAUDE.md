# Claude 知识库协作入口

本知识库的完整协作规则以根目录 [AGENTS.md](AGENTS.md) 为唯一规范源。Claude 执行任何读取、整理或修改任务前，必须完整遵循其中的语言、安全、来源、生命周期、Wiki 写作和验证规则。

特别强调：

- 始终使用简体中文。
- `raw/` 永远只读，不得修改、删除、移动、重命名或覆盖。
- 批量修改现有笔记前必须先提交计划并获得用户确认。
- 关键论断必须使用 `AGENTS.md` 规定的来源编号和精确定位。
- 不确定、过时或互相冲突的信息必须显式标记，不得自行补全为事实。
- 保持 `raw/wiki/outputs` 三层结构，不引入数据库、向量库或大量插件。
